[pytest]
# Automatically discover tests inside the 'tests/' folder
testpaths = tests

# Show verbose output by default (one line per test with PASS/FAIL)
addopts = -v --tb=short

# Required for testing async functions (used in /ask endpoint tests)
asyncio_mode = auto
